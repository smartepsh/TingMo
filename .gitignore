.DS_Store
.claude/worktrees/
.worktrees/
.superpowers/
docs/superpowers

# Xcode user state
xcuserdata/
*.xcuserstate

# Xcode build output
build/
DerivedData/

# Code coverage output from `xcodebuild test`
*.profraw

# Local release secrets
scripts/.env.release

# Local code signing overrides. This keeps macOS TCC permissions stable on a
# developer machine without committing local certificates or generated settings.
Config/LocalSigning.xcconfig
Config/TingMoLocalSigning.keychain-db
Config/.fl*

# Fiberplane local issue tracking
.fp/*
!.fp/config.toml
!.fp/extensions/
.fp/extensions/*
!.fp/extensions/*.ts
.fp/config.local.toml
